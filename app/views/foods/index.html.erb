<div class="container">
  <div class="row">
    <div class="col-md-4">
      <% @foods.each do |food| %>
        <h3><%= link_to food.name, food %></h3>
      <% end %>
      <br/>
      <%= link_to 'New', new_food_path %>
    </div>
  </div>
</div>

